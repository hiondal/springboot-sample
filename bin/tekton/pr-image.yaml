apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: pr-image-spring-sample
spec:
  type: image
  params:
    - name: url
      value: >-
        image-registry.openshift-image-registry.svc:5000/kubepia/springboot-sample

